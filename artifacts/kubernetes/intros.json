{
 "data_kubernetes_admissionregistration_mutating_admission_policy": "`kubernetes_admissionregistration_mutating_admission_policy` looks up a real, already-defined reusable rule for mutating objects as they pass through admission, expressed in CEL rather than a separately-deployed webhook server. On its own it defines the behavior; a `MutatingAdmissionPolicyBinding` decides what it actually applies to.",
 "data_kubernetes_admissionregistration_mutating_admission_policy_binding": "`kubernetes_admissionregistration_mutating_admission_policy_binding` looks up a real, already-active binding of a `MutatingAdmissionPolicy` against a specific set of resources -- the policy defines what mutation happens, the binding decides which real objects it actually happens to.",
 "data_kubernetes_admissionregistration_mutating_webhook_configuration": "`kubernetes_admissionregistration_mutating_webhook_configuration` looks up a real, already-registered external webhook the API server calls before persisting a matching object, letting that webhook modify the object on its way in -- default values, injected sidecars, anything the object itself didn't specify.",
 "data_kubernetes_admissionregistration_validating_admission_policy": "`kubernetes_admissionregistration_validating_admission_policy` looks up a real, already-defined reusable validation rule in CEL, evaluated in-process by the API server rather than by a separately-deployed webhook.",
 "data_kubernetes_admissionregistration_validating_admission_policy_binding": "`kubernetes_admissionregistration_validating_admission_policy_binding` looks up a real, already-active binding of a `ValidatingAdmissionPolicy` against a specific set of resources, the same policy-plus-binding split its mutating counterpart uses.",
 "data_kubernetes_admissionregistration_validating_webhook_configuration": "`kubernetes_admissionregistration_validating_webhook_configuration` looks up a real, already-registered external webhook the API server calls to accept or reject a matching object before it's persisted.",
 "data_kubernetes_apiextensions_custom_resource_definition": "`kubernetes_apiextensions_custom_resource_definition` looks up a real, already-registered CustomResourceDefinition -- its own schema, its own API group and version -- that the Kubernetes API server currently serves exactly like a built-in type.",
 "data_kubernetes_apiregistration_apiservice": "`kubernetes_apiregistration_apiservice` looks up a real, already-registered extension API server -- not the main one -- handling requests for one specific API group and version, which the main API server transparently proxies to it.",
 "data_kubernetes_apiserverinternal_storage_version": "`kubernetes_apiserverinternal_storage_version` looks up which real, actual API version each `kube-apiserver` replica currently uses to persist a given resource to etcd -- internal bookkeeping for safely running mixed-version API servers during a rolling upgrade.",
 "data_kubernetes_apps_controller_revision": "`kubernetes_apps_controller_revision` looks up a real, immutable snapshot of a StatefulSet or DaemonSet's own pod template at one point in time -- what a rolling update actually rolls back to if it needs to undo a change.",
 "data_kubernetes_apps_daemon_set": "`kubernetes_apps_daemon_set` looks up a real, already-running DaemonSet -- one that runs exactly one copy of a pod on every node in the cluster (or every node matching a selector), the mechanism behind node-level agents like log collectors and monitoring daemons.",
 "data_kubernetes_apps_deployment": "`kubernetes_apps_deployment` looks up a real, already-running Deployment managing a replicated set of identical pods and their own rolling update.",
 "data_kubernetes_apps_replica_set": "`kubernetes_apps_replica_set` looks up a real, already-running ReplicaSet maintaining a stable count of identical pods -- the mechanism a Deployment actually manages underneath to implement its own rolling updates.",
 "data_kubernetes_apps_stateful_set": "`kubernetes_apps_stateful_set` looks up a real, already-running StatefulSet -- pods that need a stable identity, a persistent name, a persistent network address, their own dedicated storage that follows them through a reschedule.",
 "data_kubernetes_autoscaling_horizontal_pod_autoscaler": "`kubernetes_autoscaling_horizontal_pod_autoscaler` looks up a real, already-configured HorizontalPodAutoscaler adjusting a Deployment or StatefulSet's own replica count based on observed CPU, memory, custom, or external metrics. This is the `autoscaling/v2` shape; `v1` (`kubernetes_autoscaling_v1_horizontal_pod_autoscaler`), still served for compatibility, only ever supports a single CPU-utilization target.",
 "data_kubernetes_autoscaling_scale": "`kubernetes_autoscaling_scale` reads the current replica count and selector of a scalable resource -- a Deployment, ReplicaSet, StatefulSet, or ReplicationController -- through the shared `scale` subresource every scalable API kind exposes identically, rather than each kind's own full spec.",
 "data_kubernetes_batch_cron_job": "`kubernetes_batch_cron_job` looks up a real, already-defined CronJob that creates a Job on a recurring schedule, expressed in standard cron syntax.",
 "data_kubernetes_batch_job": "`kubernetes_batch_job` looks up a real Job running one or more pods to completion for a one-off or parallelizable task, tracking real success and retrying on failure.",
 "data_kubernetes_certificates_certificate_signing_request": "`kubernetes_certificates_certificate_signing_request` looks up a real, submitted PEM-encoded certificate request awaiting or already resolved by a cluster's own CA (or an external signer) -- the mechanism kubelets and other components use to obtain real, verifiable client certificates.",
 "data_kubernetes_certificates_cluster_trust_bundle": "`kubernetes_certificates_cluster_trust_bundle` looks up a real, cluster-scoped container of X.509 root certificates, optionally tied to one specific signer, that pods can mount directly via a `clusterTrustBundle` projected volume.",
 "data_kubernetes_certificates_pod_certificate_request": "`kubernetes_certificates_pod_certificate_request` looks up a real request kubelet created on a pod's own behalf for a certificate from a specific signer, backing the `podCertificate` projected volume type.",
 "data_kubernetes_coordination_lease": "`kubernetes_coordination_lease` looks up a real, lightweight, renewable claim one component holds to prove it's still alive -- the mechanism behind node heartbeats and leader election.",
 "data_kubernetes_coordination_lease_candidate": "`kubernetes_coordination_lease_candidate` looks up a real registration of one control-plane component instance (like `kube-scheduler`) competing for leadership during coordinated leader election, carrying its own identity, binary version, and emulation version. This is the `coordination.k8s.io/v1beta1` shape; `v1alpha2` (`kubernetes_coordination_v1alpha2_lease_candidate`) is the older, still-served generation.",
 "data_kubernetes_core_component_condition": "`kubernetes_core_component_condition` reads one real health condition reported by a legacy control-plane component (`kube-scheduler`, `kube-controller-manager`, `etcd`) through the deprecated ComponentStatus API -- superseded by the componentstatuses replacement metrics and healthz endpoints most clusters use instead now.",
 "data_kubernetes_core_component_status": "`kubernetes_core_component_status` reads the real, current health of a legacy control-plane component as a whole -- the deprecated API this and `component_condition` both come from, kept for clusters that still query it rather than a `/healthz` endpoint directly.",
 "data_kubernetes_core_config_map": "`kubernetes_core_config_map` looks up real, non-sensitive configuration data -- key-value pairs or whole files -- that a pod can mount as a volume or inject as environment variables.",
 "data_kubernetes_core_endpoints": "`kubernetes_core_endpoints` looks up the real IP addresses and ports actually backing a Service -- the legacy record kube-proxy watches, superseded going forward by the more scalable EndpointSlice but still what most Services populate today.",
 "data_kubernetes_core_limit_range": "`kubernetes_core_limit_range` looks up real default and boundary constraints on compute resource requests and limits already set for every pod or container created in a namespace.",
 "data_kubernetes_core_namespace": "`kubernetes_core_namespace` looks up a real, virtual cluster inside the real cluster -- a scope for names, resource quotas, and access control.",
 "data_kubernetes_core_node": "`kubernetes_core_node` looks up one real worker machine in the cluster -- physical or virtual -- and its own real, current capacity, conditions, and the pods actually scheduled onto it.",
 "data_kubernetes_core_persistent_volume": "`kubernetes_core_persistent_volume` looks up a real piece of storage provisioned in the cluster, independent of any one pod's own lifecycle, and bound to exactly one PersistentVolumeClaim at a time.",
 "data_kubernetes_core_persistent_volume_claim": "`kubernetes_core_persistent_volume_claim` looks up a real request for storage made by a pod -- how much, what access mode, optionally which StorageClass -- bound to a matching PersistentVolume.",
 "data_kubernetes_core_pod": "`kubernetes_core_pod` looks up the smallest real, deployable unit in Kubernetes -- one or more containers that share the same network namespace and storage, scheduled, started, and stopped together as a single real thing.",
 "data_kubernetes_core_pod_template": "`kubernetes_core_pod_template` looks up a real, reusable pod specification embedded inside a higher-level controller -- a Deployment, a Job -- the actual blueprint the controller stamps out real pods from.",
 "data_kubernetes_core_replication_controller": "`kubernetes_core_replication_controller` looks up a real ReplicationController, the original mechanism for keeping a stable count of identical pods running -- functionally superseded by ReplicaSet (and, above that, Deployment) for new workloads, but still real, supported API surface.",
 "data_kubernetes_core_secret": "`kubernetes_core_secret` looks up real sensitive data -- a password, a token, a TLS certificate -- stored separately from a ConfigMap specifically so it can be handled with tighter access control.",
 "data_kubernetes_core_service": "`kubernetes_core_service` looks up a real, stable network identity -- a virtual IP and DNS name -- already assigned to a set of pods selected by label.",
 "data_kubernetes_core_service_account": "`kubernetes_core_service_account` looks up a real identity a pod authenticates to the Kubernetes API as -- distinct from a human user account -- what RBAC RoleBindings actually grant permissions to when a workload, not a person, needs to call the API.",
 "data_kubernetes_discovery_endpoint_slice": "`kubernetes_discovery_endpoint_slice` looks up a real, scalable replacement for the older Endpoints resource -- one slice of a Service's own backing endpoints, split across multiple smaller objects so a Service with thousands of real pods doesn't force every watcher to process one enormous, constantly-changing record.",
 "data_kubernetes_flowcontrol_flow_schema": "`kubernetes_flowcontrol_flow_schema` looks up a real rule that classifies incoming API server requests into a priority level based on who's making them -- part of API Priority and Fairness.",
 "data_kubernetes_flowcontrol_priority_level_configuration": "`kubernetes_flowcontrol_priority_level_configuration` looks up the real concurrency budget one priority level of API requests actually gets -- how many can run at once, how the rest queue -- the other half of API Priority and Fairness alongside FlowSchema.",
 "data_kubernetes_io_k8s_apimachinery_pkg_version_info": "`kubernetes_io_k8s_apimachinery_pkg_version_info` reads the real Kubernetes version the API server is currently running -- major, minor, git version, build date, platform -- the same information `kubectl version` reports, useful for a stack that needs to branch on cluster version rather than assume one.",
 "data_kubernetes_lifecycle_eviction": "`kubernetes_lifecycle_eviction` looks up a real, requested graceful removal of a pod, respecting that pod's own PodDisruptionBudget rather than deleting it outright -- what `kubectl drain` and the cluster autoscaler both create.",
 "data_kubernetes_lifecycle_eviction_request": "`kubernetes_lifecycle_eviction_request` looks up a real, coordinated eviction request that lets a workload controller intercept a pending eviction and migrate the pod gracefully before falling back to a standard eviction if nothing intercepts it in time.",
 "data_kubernetes_meta_apigroup": "`kubernetes_meta_apigroup` reads one real API group the cluster's API server currently serves -- its name and the API versions available within it -- part of the same discovery mechanism `kubectl api-versions` uses.",
 "data_kubernetes_meta_apiresource": "`kubernetes_meta_apiresource` reads one real resource kind the cluster's API server currently serves within a given API group and version -- whether it's namespaced, what verbs it supports -- part of the same discovery mechanism `kubectl api-resources` uses.",
 "data_kubernetes_meta_server_address_by_client_cidr": "`kubernetes_meta_server_address_by_client_cidr` reads which real API server address a client within a given CIDR range should actually use to reach the cluster -- relevant for a cluster that advertises a different server address depending on where the client is connecting from.",
 "data_kubernetes_networking_ingress": "`kubernetes_networking_ingress` looks up a real routing rule for external HTTP(S) traffic into Services inside the cluster, based on hostname and path.",
 "data_kubernetes_networking_ingress_class": "`kubernetes_networking_ingress_class` looks up one real Ingress controller implementation -- nginx, a cloud load balancer, another -- that an Ingress resource can specify should handle it.",
 "data_kubernetes_networking_ipaddress": "`kubernetes_networking_ipaddress` looks up one real IP address actually allocated to a Service, tracked as its own object rather than implicitly -- part of the newer allocator that lets a cluster's own set of usable Service IPs grow by adding another ServiceCIDR.",
 "data_kubernetes_networking_network_policy": "`kubernetes_networking_network_policy` looks up a real restriction on network traffic to and from a set of pods, selected by label -- a cluster-native firewall between workloads, enforced by whichever CNI plugin the cluster runs.",
 "data_kubernetes_networking_service_cidr": "`kubernetes_networking_service_cidr` looks up one real, additional range of IP addresses available for allocating to Services -- a cluster can define more than one, and the allocator draws from whichever has room.",
 "data_kubernetes_node_runtime_class": "`kubernetes_node_runtime_class` looks up which real container runtime configuration a pod actually runs under -- standard runc, or a sandboxed alternative like gVisor or Kata Containers.",
 "data_kubernetes_policy_pod_disruption_budget": "`kubernetes_policy_pod_disruption_budget` looks up a real floor on how many pods of an application can be voluntarily disrupted at once -- a node drain, a cluster upgrade -- protecting availability during planned maintenance.",
 "data_kubernetes_rbac_cluster_role": "`kubernetes_rbac_cluster_role` looks up a real set of permissions that apply cluster-wide, across every namespace, or to cluster-scoped resources that don't belong to any namespace at all.",
 "data_kubernetes_rbac_cluster_role_binding": "`kubernetes_rbac_cluster_role_binding` looks up a real grant of a ClusterRole's own permissions to a specific user, group, or service account across the whole cluster -- the role alone grants nothing on its own.",
 "data_kubernetes_rbac_role": "`kubernetes_rbac_role` looks up a real set of permissions scoped to one specific namespace.",
 "data_kubernetes_rbac_role_binding": "`kubernetes_rbac_role_binding` looks up a real grant of a Role's own permissions to a specific user, group, or service account inside that Role's own namespace.",
 "data_kubernetes_resource_device_class": "`kubernetes_resource_device_class` looks up a real category of hardware devices -- a specific GPU model, a class of network accelerator -- that Dynamic Resource Allocation matches a workload's own request against.",
 "data_kubernetes_resource_device_taint_rule": "`kubernetes_resource_device_taint_rule` looks up a real taint already applied to a device managed through Dynamic Resource Allocation, marking it unavailable for new claims unless a workload explicitly tolerates that taint.",
 "data_kubernetes_resource_resource_claim": "`kubernetes_resource_resource_claim` looks up a real, individual request for a specific device or set of devices under Dynamic Resource Allocation -- what a pod actually references to get a real, matched piece of hardware allocated to it, the DRA analog of a PersistentVolumeClaim for compute devices instead of storage.",
 "data_kubernetes_resource_resource_claim_template": "`kubernetes_resource_resource_claim_template` looks up a real, reusable template for generating a ResourceClaim automatically per pod -- so a Deployment with many replicas doesn't need a hand-written claim for every single one.",
 "data_kubernetes_resource_resource_pool_status_request": "`kubernetes_resource_resource_pool_status_request` looks up a real, point-in-time snapshot of device availability -- total, allocated, available, unavailable -- for a specific driver's own resource pool under Dynamic Resource Allocation.",
 "data_kubernetes_resource_resource_slice": "`kubernetes_resource_resource_slice` looks up the real, current inventory of devices one driver makes available for Dynamic Resource Allocation on a node or in a pool -- what DRA's own scheduling logic actually matches ResourceClaims against.",
 "data_kubernetes_scheduling_composite_pod_group": "`kubernetes_scheduling_composite_pod_group` looks up a real aggregation of multiple PodGroups into one higher-level gang-scheduling unit -- for a workload made of several distinct pod groups that still all need to be scheduled together or not at all.",
 "data_kubernetes_scheduling_pod_group": "`kubernetes_scheduling_pod_group` looks up the real, runtime instance of a group of pods that must be scheduled together as a single unit -- if the scheduler can't place every pod in the group, none of them get bound. This is the `scheduling.k8s.io/v1beta1` shape; `v1alpha3` (`kubernetes_scheduling_v1alpha3_pod_group`) is the older, still-served generation.",
 "data_kubernetes_scheduling_priority_class": "`kubernetes_scheduling_priority_class` looks up a real priority level pods can reference, which the scheduler uses to decide which pending pod gets scheduled first and which lower-priority pods can be preempted.",
 "data_kubernetes_scheduling_workload": "`kubernetes_scheduling_workload` looks up the real, static template a workload controller (a Job, a JobSet, and similar) generates its own PodGroups from. This is the `scheduling.k8s.io/v1beta1` shape; `v1alpha3` (`kubernetes_scheduling_v1alpha3_workload`) is the older, still-served generation.",
 "data_kubernetes_storage_csidriver": "`kubernetes_storage_csidriver` looks up a real, already-registered Container Storage Interface driver, declaring what it actually supports -- volume attach/detach, expansion, snapshotting.",
 "data_kubernetes_storage_csinode": "`kubernetes_storage_csinode` looks up which real CSI drivers are actually available on one specific node, and each driver's own node-specific limits.",
 "data_kubernetes_storage_csistorage_capacity": "`kubernetes_storage_csistorage_capacity` looks up the real, currently available storage capacity a CSI driver can provision within a given topology segment.",
 "data_kubernetes_storage_storage_class": "`kubernetes_storage_storage_class` looks up one real class of storage a cluster can dynamically provision -- which driver, what parameters, what reclaim policy.",
 "data_kubernetes_storage_volume_attachment": "`kubernetes_storage_volume_attachment` looks up the real, internal record of one volume actually attached to one node -- API-server-managed detail a CSI driver and the attach/detach controller use to coordinate.",
 "data_kubernetes_storage_volume_attributes_class": "`kubernetes_storage_volume_attributes_class` looks up a real, mutable set of volume attributes a CSI driver applies during provisioning or a later modification.",
 "data_kubernetes_storagemigration_storage_version_migration": "`kubernetes_storagemigration_storage_version_migration` looks up the real, cluster-wide status of a rewrite of every existing object of a given resource type to its current storage version.",
 "kubernetes_admissionregistration_mutating_admission_policy": "`kubernetes_admissionregistration_mutating_admission_policy` defines a reusable, in-process rule for mutating objects as they pass through admission, expressed in CEL rather than requiring a real, separately-deployed webhook server. It needs a `MutatingAdmissionPolicyBinding` to actually activate against real objects -- the policy alone only defines the behavior, the binding decides what it applies to.",
 "kubernetes_admissionregistration_mutating_admission_policy_binding": "`kubernetes_admissionregistration_mutating_admission_policy_binding` activates a `MutatingAdmissionPolicy` against a real, specific set of resources -- the policy defines what mutation happens, the binding decides which real objects it actually happens to.",
 "kubernetes_admissionregistration_mutating_webhook_configuration": "`kubernetes_admissionregistration_mutating_webhook_configuration` registers a real, external webhook the API server calls before persisting a matching object, letting that webhook modify the object on its way in -- default values, injected sidecars, anything the object itself didn't specify.",
 "kubernetes_admissionregistration_v1alpha1_mutating_admission_policy": "The v1alpha1 shape of `kubernetes_admissionregistration_mutating_admission_policy` -- MutatingAdmissionPolicy's own original, first-shipped API version (Kubernetes 1.32), from before the feature moved through v1beta1 (1.34) to its current stable v1 (1.36). Kept being served for clusters still migrating off it; new policies should target the stable `kubernetes_admissionregistration_mutating_admission_policy` instead.",
 "kubernetes_admissionregistration_v1alpha1_mutating_admission_policy_binding": "The v1alpha1 shape of `kubernetes_admissionregistration_mutating_admission_policy_binding` -- the same real MutatingAdmissionPolicyBinding concept, from MutatingAdmissionPolicy's own original alpha API generation before it graduated through beta to a stable v1. New bindings should target the stable `kubernetes_admissionregistration_mutating_admission_policy_binding` instead.",
 "kubernetes_admissionregistration_v1beta1_mutating_admission_policy": "The v1beta1 shape of `kubernetes_admissionregistration_mutating_admission_policy` -- MutatingAdmissionPolicy's own beta API generation (Kubernetes 1.34), one step past the original v1alpha1 and one step before the now-stable v1. Kept being served during the deprecation window; new policies should target the stable `kubernetes_admissionregistration_mutating_admission_policy` instead.",
 "kubernetes_admissionregistration_v1beta1_mutating_admission_policy_binding": "The v1beta1 shape of `kubernetes_admissionregistration_mutating_admission_policy_binding` -- MutatingAdmissionPolicyBinding's own beta API generation, one step past the original v1alpha1 and one step before the now-stable v1. New bindings should target the stable `kubernetes_admissionregistration_mutating_admission_policy_binding` instead.",
 "kubernetes_admissionregistration_validating_admission_policy": "`kubernetes_admissionregistration_validating_admission_policy` defines a reusable validation rule in CEL, evaluated in-process by the API server rather than by a real, separately-deployed webhook -- the same real job a validating webhook does, without the extra network hop or the availability risk of running one.",
 "kubernetes_admissionregistration_validating_admission_policy_binding": "`kubernetes_admissionregistration_validating_admission_policy_binding` activates a `ValidatingAdmissionPolicy` against a real, specific set of resources, the same real policy-plus-binding split its mutating counterpart uses.",
 "kubernetes_admissionregistration_validating_webhook_configuration": "`kubernetes_admissionregistration_validating_webhook_configuration` registers a real, external webhook the API server calls to accept or reject a matching object before it's persisted -- a real, custom admission check beyond what the API server's own built-in validation covers.",
 "kubernetes_apiextensions_custom_resource_definition": "`kubernetes_apiextensions_custom_resource_definition` defines a real, new resource type -- its own schema, its own API group and version -- that the Kubernetes API server then serves exactly like a built-in type. It's the real foundation every operator and every custom controller pattern in the ecosystem is built on.",
 "kubernetes_apiregistration_apiservice": "`kubernetes_apiregistration_apiservice` registers a real, separate API server -- an extension API server, not the main one -- to handle requests for one specific API group and version, letting the main API server transparently proxy those requests to it.",
 "kubernetes_apiserverinternal_storage_version": "`kubernetes_apiserverinternal_storage_version` records which real, actual API version each `kube-apiserver` replica currently uses to persist a given resource to etcd -- the internal bookkeeping that makes it safe to run mixed-version API servers during a rolling upgrade without silently corrupting stored data.",
 "kubernetes_apps_controller_revision": "`kubernetes_apps_controller_revision` is a real, immutable snapshot of a StatefulSet or DaemonSet's own pod template at one point in time -- what a rolling update actually rolls back to if it needs to undo a change.",
 "kubernetes_apps_daemon_set": "`kubernetes_apps_daemon_set` runs exactly one copy of a pod on every real node in the cluster (or every node matching a selector) -- the mechanism behind node-level agents like log collectors and monitoring daemons that need to be everywhere, not scaled to an arbitrary count.",
 "kubernetes_apps_deployment": "`kubernetes_apps_deployment` manages a real, replicated set of identical pods and their own rolling update, declaratively -- change the pod template, and the Deployment controller works out how to get from the old real state to the new one without downtime.",
 "kubernetes_apps_replica_set": "`kubernetes_apps_replica_set` maintains a real, stable count of identical running pods -- the mechanism a Deployment actually manages underneath to implement its own rolling updates, rarely created directly since a Deployment already does that job.",
 "kubernetes_apps_stateful_set": "`kubernetes_apps_stateful_set` manages pods that need a real, stable identity -- a persistent name, a persistent network address, their own dedicated storage that follows them through a reschedule -- the real fit for databases and anything else that can't treat its own instances as interchangeable.",
 "kubernetes_autoscaling_horizontal_pod_autoscaler": "`kubernetes_autoscaling_horizontal_pod_autoscaler` automatically adjusts a real Deployment or StatefulSet's own replica count based on observed CPU, memory, custom, or external metrics -- scaling out under real load and back in once it passes, without a human watching a dashboard. This is the `autoscaling/v2` shape, the real, current, preferred one -- Kubernetes' own per-API-group discovery signal confirms it, and it's the one this plain, unversioned typeName now resolves to. `v1` (`kubernetes_autoscaling_v1_horizontal_pod_autoscaler`), still served for compatibility, only ever supports a single CPU-utilization target; this v2 shape replaces that with a real `metrics` array supporting multiple concurrent metrics at once, plus real status conditions reporting whether the autoscaler is actually able to scale right now.",
 "kubernetes_autoscaling_v1_horizontal_pod_autoscaler": "The real, older `autoscaling/v1` shape of `kubernetes_autoscaling_horizontal_pod_autoscaler`, still served for compatibility but not the current, preferred one -- that's the plain, unversioned sibling (`autoscaling/v2`). v1 only ever supports scaling on a single CPU-utilization target (`target_cpu_utilization_percentage`), predating v2's own multi-metric `metrics` array and real scaling status conditions. New HorizontalPodAutoscalers should target the plain, unversioned resource instead.",
 "kubernetes_batch_cron_job": "`kubernetes_batch_cron_job` creates a real Job on a recurring schedule, expressed in standard cron syntax -- the mechanism behind scheduled backups, report generation, and any other task that needs to run repeatedly rather than once.",
 "kubernetes_batch_job": "`kubernetes_batch_job` runs one or more pods to completion for a real, one-off or parallelizable task, tracking real success and retrying on failure -- the fit for a batch computation or a migration script, not a long-running service.",
 "kubernetes_certificates_certificate_signing_request": "`kubernetes_certificates_certificate_signing_request` submits a real PEM-encoded certificate request for a cluster's own CA (or an external signer) to approve and sign -- the mechanism kubelets and other components use to obtain real, verifiable client certificates without a human manually running `openssl`.",
 "kubernetes_certificates_cluster_trust_bundle": "`kubernetes_certificates_cluster_trust_bundle` is a cluster-scoped container for real X.509 root certificates, optionally tied to one specific signer, that pods can mount directly via a `clusterTrustBundle` projected volume -- a real, native way to distribute trusted CA bundles without baking them into every image.",
 "kubernetes_certificates_pod_certificate_request": "`kubernetes_certificates_pod_certificate_request` is what kubelet creates on a pod's own behalf to request a real certificate from a specific signer, backing the `podCertificate` projected volume type -- Kubernetes itself never issues the certificate, an external controller watching these requests does.",
 "kubernetes_certificates_v1beta1_cluster_trust_bundle": "The v1beta1 shape of `kubernetes_certificates_cluster_trust_bundle` -- ClusterTrustBundle's own beta API generation, kept being served alongside the now-stable v1 API this cluster's own pinned schema also carries. New trust bundles should target the stable `kubernetes_certificates_cluster_trust_bundle` instead.",
 "kubernetes_certificates_v1beta1_pod_certificate_request": "The v1beta1 shape of `kubernetes_certificates_pod_certificate_request` -- PodCertificateRequest's own beta API generation (introduced Kubernetes 1.35), kept being served alongside the now-stable v1 API kubelet itself has since moved onto. New certificate requests should target the stable `kubernetes_certificates_pod_certificate_request` instead.",
 "kubernetes_coordination_lease": "`kubernetes_coordination_lease` is a real, lightweight, renewable claim one component holds to prove it's still alive -- the mechanism behind node heartbeats and leader election, where losing the lease within its own timeout is what tells the rest of the system to act.",
 "kubernetes_coordination_lease_candidate": "`kubernetes_coordination_lease_candidate` registers one real instance of a control-plane component (like `kube-scheduler`) as competing for leadership during coordinated leader election, carrying its own identity, binary version, and emulation version -- real information the cluster uses to deterministically pick a leader during a rolling upgrade rather than picking whichever instance happened to grab the lease first. This is the `coordination.k8s.io/v1beta1` shape, the real, current generation control-plane components are moving onto -- Coordinated Leader Election's original API generation was `v1alpha1`, then `v1alpha2` (`kubernetes_coordination_v1alpha2_lease_candidate`, still served but older), superseded by this v1beta1 generation.",
 "kubernetes_coordination_v1alpha2_lease_candidate": "The real, older `coordination.k8s.io/v1alpha2` shape of `kubernetes_coordination_lease_candidate`, still served for compatibility but not the current one -- that's the plain, unversioned sibling (`v1beta1`), the generation control-plane components are actually moving onto for Coordinated Leader Election.",
 "kubernetes_core_config_map": "`kubernetes_core_config_map` stores real, non-sensitive configuration data -- key-value pairs or whole files -- that a pod can mount as a volume or inject as environment variables, decoupling configuration from the container image that uses it.",
 "kubernetes_core_endpoints": "`kubernetes_core_endpoints` lists the real IP addresses and ports actually backing a Service -- the legacy record kube-proxy watches to know where to route traffic, superseded going forward by the more scalable EndpointSlice but still the resource most Services populate today.",
 "kubernetes_core_event": "`kubernetes_core_event` is a real record of something that happened to another object in the cluster -- a pod scheduled, a container failed to pull its image -- the audit trail `kubectl describe` surfaces to explain why a resource is in the state it's in.",
 "kubernetes_core_limit_range": "`kubernetes_core_limit_range` sets real default and boundary constraints on compute resource requests and limits for every pod or container created in a namespace, so a workload that forgets to specify its own limits doesn't silently consume unbounded resources.",
 "kubernetes_core_namespace": "`kubernetes_core_namespace` is a real, virtual cluster inside the real cluster -- a scope for names, resource quotas, and access control that lets multiple teams or environments share one Kubernetes cluster without their own resources colliding.",
 "kubernetes_core_node": "`kubernetes_core_node` represents one real worker machine in the cluster -- physical or virtual -- and its own real, current capacity, conditions, and the pods actually scheduled onto it.",
 "kubernetes_core_persistent_volume": "`kubernetes_core_persistent_volume` represents a real piece of storage provisioned in the cluster, independent of any one pod's own lifecycle -- created either ahead of time by an administrator or dynamically by a StorageClass, and bound to exactly one PersistentVolumeClaim at a time.",
 "kubernetes_core_persistent_volume_claim": "`kubernetes_core_persistent_volume_claim` is a real request for storage made by a pod -- how much, what access mode, optionally which StorageClass -- that Kubernetes binds to a matching (or dynamically provisioned) PersistentVolume on the workload's own behalf.",
 "kubernetes_core_pod": "`kubernetes_core_pod` is the smallest real, deployable unit in Kubernetes -- one or more containers that share the same network namespace and storage, always scheduled, started, and stopped together as a single real thing.",
 "kubernetes_core_pod_template": "`kubernetes_core_pod_template` is a real, reusable pod specification embedded inside a higher-level controller -- a Deployment, a Job -- the actual blueprint the controller stamps out real pods from.",
 "kubernetes_core_replication_controller": "`kubernetes_core_replication_controller` is the original, real mechanism for keeping a stable count of identical pods running -- functionally superseded by ReplicaSet (and, above that, Deployment) for new workloads, but still real, supported API surface.",
 "kubernetes_core_resource_quota": "`kubernetes_core_resource_quota` caps the real total compute, storage, and object-count consumption a namespace is allowed -- the mechanism that stops one team's own namespace from starving every other namespace sharing the same cluster.",
 "kubernetes_core_secret": "`kubernetes_core_secret` stores real sensitive data -- a password, a token, a TLS certificate -- separately from a ConfigMap specifically so it can be handled with tighter real access control, mounted or injected into only the pods that actually need it.",
 "kubernetes_core_service": "`kubernetes_core_service` gives a real, stable network identity -- a virtual IP and DNS name -- to a set of pods selected by label, so other workloads can reach them without tracking individual pod IPs that change every time a pod is rescheduled.",
 "kubernetes_core_service_account": "`kubernetes_core_service_account` is a real identity a pod authenticates to the Kubernetes API as -- distinct from a human user account -- and is what RBAC RoleBindings actually grant permissions to when a workload, not a person, needs to call the API.",
 "kubernetes_discovery_endpoint_slice": "`kubernetes_discovery_endpoint_slice` is the real, scalable replacement for the older Endpoints resource -- splitting a Service's own backing endpoints across multiple, smaller objects so a Service with thousands of real pods doesn't force every watcher to process one enormous, constantly-changing record.",
 "kubernetes_events_event": "`kubernetes_events_event` is the same real event record as the original core Event resource, exposed through the newer, dedicated `events.k8s.io` API group -- a real schema cleanup, not a different concept.",
 "kubernetes_flowcontrol_flow_schema": "`kubernetes_flowcontrol_flow_schema` classifies incoming API server requests into a real priority level based on who's making them -- the mechanism behind API Priority and Fairness, which stops one noisy client from starving every other real client's own requests during a busy period.",
 "kubernetes_flowcontrol_priority_level_configuration": "`kubernetes_flowcontrol_priority_level_configuration` defines the real concurrency budget one priority level of API requests actually gets -- how many can run at once, how the rest queue -- the other real half of API Priority and Fairness alongside FlowSchema.",
 "kubernetes_lifecycle_eviction": "`kubernetes_lifecycle_eviction` requests the real, graceful removal of a pod, respecting that pod's own PodDisruptionBudget rather than deleting it outright -- the mechanism `kubectl drain` and the cluster autoscaler both use so a voluntary disruption doesn't take down more of an application than its own budget allows.",
 "kubernetes_lifecycle_eviction_request": "`kubernetes_lifecycle_eviction_request` is a newer, coordinated eviction mechanism that lets a workload controller intercept a pending eviction and migrate the real pod gracefully -- checkpointing state, moving a connection -- before falling back to a real, standard eviction if nothing intercepts it in time.",
 "kubernetes_networking_ingress": "`kubernetes_networking_ingress` routes external HTTP(S) traffic to real Services inside the cluster based on hostname and path -- a single real entry point that can front many different backend Services without giving each one its own external load balancer.",
 "kubernetes_networking_ingress_class": "`kubernetes_networking_ingress_class` names one real Ingress controller implementation -- nginx, an cloud load balancer, another -- so an Ingress resource can specify which real controller should actually handle it when a cluster runs more than one.",
 "kubernetes_networking_ipaddress": "`kubernetes_networking_ipaddress` records one real IP address actually allocated to a Service, tracked as its own object rather than implicitly -- part of the newer allocator that lets a cluster's own set of usable Service IPs grow by adding another ServiceCIDR instead of being fixed at cluster creation.",
 "kubernetes_networking_network_policy": "`kubernetes_networking_network_policy` restricts real network traffic to and from a set of pods, selected by label -- the mechanism for a real, cluster-native firewall between workloads, enforced by whichever CNI plugin the cluster runs.",
 "kubernetes_networking_service_cidr": "`kubernetes_networking_service_cidr` declares one real, additional range of IP addresses available for allocating to Services -- a cluster can define more than one, and the allocator draws from whichever has room, the mechanism that makes a cluster's own Service IP space extensible after the fact.",
 "kubernetes_node_runtime_class": "`kubernetes_node_runtime_class` selects which real container runtime configuration a pod actually runs under -- standard runc, or a sandboxed alternative like gVisor or Kata Containers -- when a cluster's own nodes support more than one.",
 "kubernetes_policy_pod_disruption_budget": "`kubernetes_policy_pod_disruption_budget` sets a real floor on how many pods of an application can be voluntarily disrupted at once -- a node drain, a cluster upgrade -- protecting availability during planned maintenance the same way replica count protects it during an unplanned failure.",
 "kubernetes_rbac_cluster_role": "`kubernetes_rbac_cluster_role` defines a real set of permissions that apply cluster-wide, across every namespace, or to cluster-scoped resources that don't belong to any namespace at all -- the RBAC building block for something like `view all pods everywhere`, not one namespace's own scope.",
 "kubernetes_rbac_cluster_role_binding": "`kubernetes_rbac_cluster_role_binding` grants a real ClusterRole's own permissions to a specific user, group, or service account across the whole cluster -- the RBAC resource that actually activates a ClusterRole; the role alone grants nothing on its own.",
 "kubernetes_rbac_role": "`kubernetes_rbac_role` defines a real set of permissions scoped to one specific namespace -- `edit deployments in this namespace only`, not the whole cluster.",
 "kubernetes_rbac_role_binding": "`kubernetes_rbac_role_binding` grants a real Role's own permissions to a specific user, group, or service account inside that Role's own namespace -- the resource that actually activates a Role.",
 "kubernetes_resource_device_class": "`kubernetes_resource_device_class` groups a real category of hardware devices -- a specific GPU model, a class of network accelerator -- that Dynamic Resource Allocation can then match a workload's own request against, the real taxonomy DRA's whole request-and-match mechanism is built on.",
 "kubernetes_resource_device_taint_rule": "`kubernetes_resource_device_taint_rule` taints a real device managed through Dynamic Resource Allocation, marking it unavailable for new claims unless a workload explicitly tolerates that taint -- the DRA equivalent of tainting a node, applied to individual hardware devices instead.",
 "kubernetes_resource_resource_claim": "`kubernetes_resource_resource_claim` is a real, individual request for a specific device or set of devices under Dynamic Resource Allocation -- what a pod actually references to get a real, matched piece of hardware allocated to it, the DRA analog of a PersistentVolumeClaim for compute devices instead of storage.",
 "kubernetes_resource_resource_claim_template": "`kubernetes_resource_resource_claim_template` is a real, reusable template for generating a ResourceClaim automatically per pod -- so a Deployment with many replicas doesn't need a hand-written claim for every single one.",
 "kubernetes_resource_resource_pool_status_request": "`kubernetes_resource_resource_pool_status_request` asks for a real, point-in-time snapshot of device availability -- total, allocated, available, unavailable -- for a specific driver's own resource pool under Dynamic Resource Allocation, the mechanism for actually checking real hardware capacity before assuming a claim will succeed.",
 "kubernetes_resource_resource_slice": "`kubernetes_resource_resource_slice` publishes the real, current inventory of devices one driver makes available for Dynamic Resource Allocation on a node or in a pool -- what DRA's own scheduling logic actually matches ResourceClaims against.",
 "kubernetes_resource_v1alpha3_device_taint_rule": "The v1alpha3 shape of `kubernetes_resource_device_taint_rule` -- DeviceTaintRule's original, alpha-numbered API version string (KEP-5055), kept even after the feature itself reached beta-quality stability in Kubernetes 1.36 without a matching version-string rename. Kept being served alongside the resource's own current, stable v1 API this cluster's own pinned schema also carries; new taint rules should target the stable `kubernetes_resource_device_taint_rule` instead.",
 "kubernetes_resource_v1beta1_device_class": "The v1beta1 shape of `kubernetes_resource_device_class` -- Dynamic Resource Allocation's own pre-GA API generation, one step before the resource.k8s.io/v1beta2 shape and two before the now-stable v1 API DRA's core graduated to in Kubernetes 1.34. Kept being served during the deprecation window; new device classes should target the stable `kubernetes_resource_device_class` instead.",
 "kubernetes_resource_v1beta1_resource_claim": "The v1beta1 shape of `kubernetes_resource_resource_claim` -- Dynamic Resource Allocation's own pre-GA API generation, one step before the resource.k8s.io/v1beta2 shape and two before the now-stable v1 API DRA's core graduated to in Kubernetes 1.34. Kept being served during the deprecation window; new claims should target the stable `kubernetes_resource_resource_claim` instead.",
 "kubernetes_resource_v1beta1_resource_claim_template": "The v1beta1 shape of `kubernetes_resource_resource_claim_template` -- Dynamic Resource Allocation's own pre-GA API generation, one step before the resource.k8s.io/v1beta2 shape and two before the now-stable v1 API DRA's core graduated to in Kubernetes 1.34. Kept being served during the deprecation window; new templates should target the stable `kubernetes_resource_resource_claim_template` instead.",
 "kubernetes_resource_v1beta1_resource_slice": "The v1beta1 shape of `kubernetes_resource_resource_slice` -- Dynamic Resource Allocation's own pre-GA API generation, one step before the resource.k8s.io/v1beta2 shape and two before the now-stable v1 API DRA's core graduated to in Kubernetes 1.34. Kept being served during the deprecation window; new inventory should be published through the stable `kubernetes_resource_resource_slice` instead.",
 "kubernetes_resource_v1beta2_device_class": "The v1beta2 shape of `kubernetes_resource_device_class` -- Dynamic Resource Allocation's own later pre-GA API generation, one step past v1beta1 and one step before the now-stable v1 API DRA's core graduated to in Kubernetes 1.34. Kept being served during the deprecation window; new device classes should target the stable `kubernetes_resource_device_class` instead.",
 "kubernetes_resource_v1beta2_device_taint_rule": "The v1beta2 shape of `kubernetes_resource_device_taint_rule` -- DeviceTaintRule's own later pre-GA API generation, matching Dynamic Resource Allocation's shared v1beta2 versioning cadence (DeviceTaintRule was itself introduced later than DRA's other core types, starting at v1alpha3, then moving straight to v1beta2 rather than through an intermediate v1beta1 of its own). Kept being served during the deprecation window; new taint rules should target the stable `kubernetes_resource_device_taint_rule` instead.",
 "kubernetes_resource_v1beta2_resource_claim": "The v1beta2 shape of `kubernetes_resource_resource_claim` -- Dynamic Resource Allocation's own later pre-GA API generation, one step past v1beta1 and one step before the now-stable v1 API DRA's core graduated to in Kubernetes 1.34. Kept being served during the deprecation window; new claims should target the stable `kubernetes_resource_resource_claim` instead.",
 "kubernetes_resource_v1beta2_resource_claim_template": "The v1beta2 shape of `kubernetes_resource_resource_claim_template` -- Dynamic Resource Allocation's own later pre-GA API generation, one step past v1beta1 and one step before the now-stable v1 API DRA's core graduated to in Kubernetes 1.34. Kept being served during the deprecation window; new templates should target the stable `kubernetes_resource_resource_claim_template` instead.",
 "kubernetes_resource_v1beta2_resource_slice": "The v1beta2 shape of `kubernetes_resource_resource_slice` -- Dynamic Resource Allocation's own later pre-GA API generation, one step past v1beta1 and one step before the now-stable v1 API DRA's core graduated to in Kubernetes 1.34. Kept being served during the deprecation window; new inventory should be published through the stable `kubernetes_resource_resource_slice` instead.",
 "kubernetes_scheduling_composite_pod_group": "`kubernetes_scheduling_composite_pod_group` aggregates multiple real PodGroups into one higher-level gang-scheduling unit -- for a workload made of several distinct pod groups that still all need to be scheduled together or not at all.",
 "kubernetes_scheduling_pod_group": "`kubernetes_scheduling_pod_group` is the real, runtime instance of a group of pods that must be scheduled together as a single unit -- if the scheduler can't place every pod in the group, none of them get bound, the mechanism behind real gang scheduling for distributed training and similar all-or-nothing workloads. This is the `scheduling.k8s.io/v1beta1` shape, the real, current generation gang-scheduled workloads should target -- the Workload/PodGroup APIs moved from an original `v1alpha2` generation (entirely removed in a later release) to `v1alpha3` (`kubernetes_scheduling_v1alpha3_pod_group`, still served but older) to this v1beta1 generation.",
 "kubernetes_scheduling_priority_class": "`kubernetes_scheduling_priority_class` names a real priority level pods can reference, which the scheduler uses to decide which pending pod gets scheduled first, and which lower-priority pods can be preempted to make room for a higher-priority one under real resource pressure.",
 "kubernetes_scheduling_v1alpha3_pod_group": "The real, older `scheduling.k8s.io/v1alpha3` shape of `kubernetes_scheduling_pod_group`, still served for compatibility but not the current one -- that's the plain, unversioned sibling (`v1beta1`), the generation new gang-scheduled workloads should actually target.",
 "kubernetes_scheduling_v1alpha3_workload": "The real, older `scheduling.k8s.io/v1alpha3` shape of `kubernetes_scheduling_workload`, still served for compatibility but not the current one -- that's the plain, unversioned sibling (`v1beta1`), the generation new gang-scheduled workload templates should actually target.",
 "kubernetes_scheduling_workload": "`kubernetes_scheduling_workload` is the real, static template a workload controller (a Job, a JobSet, and similar) generates its own PodGroups from -- the definition of how pods should be grouped for gang scheduling, separate from the PodGroup's own runtime state. This is the `scheduling.k8s.io/v1beta1` shape, the real, current generation gang-scheduled workload templates should target -- the Workload/PodGroup APIs moved from an original `v1alpha2` generation (entirely removed in a later release) to `v1alpha3` (`kubernetes_scheduling_v1alpha3_workload`, still served but older) to this v1beta1 generation.",
 "kubernetes_storage_csidriver": "`kubernetes_storage_csidriver` registers a real Container Storage Interface driver with the cluster, declaring what it actually supports -- volume attach/detach, expansion, snapshotting -- so Kubernetes knows which real operations it can ask that driver to perform.",
 "kubernetes_storage_csinode": "`kubernetes_storage_csinode` records which real CSI drivers are actually available on one specific node, and each driver's own node-specific limits -- the information the scheduler needs to avoid placing a pod on a node that can't actually attach the storage it requires.",
 "kubernetes_storage_csistorage_capacity": "`kubernetes_storage_csistorage_capacity` reports the real, currently available storage capacity a CSI driver can provision within a given topology segment -- what lets the scheduler avoid placing a pod's storage request onto a node or zone that's actually already full.",
 "kubernetes_storage_storage_class": "`kubernetes_storage_storage_class` describes one real class of storage a cluster can dynamically provision -- which driver, what parameters, what reclaim policy -- so a PersistentVolumeClaim can request `fast SSD storage` by name instead of an administrator manually creating a matching PersistentVolume ahead of time.",
 "kubernetes_storage_volume_attachment": "`kubernetes_storage_volume_attachment` is the real, internal record of one volume actually attached to one node -- API-server-managed detail a CSI driver and the attach/detach controller use to coordinate, not something a workload references directly.",
 "kubernetes_storage_volume_attributes_class": "`kubernetes_storage_volume_attributes_class` defines a real, mutable set of volume attributes a CSI driver applies during provisioning or a later modification -- letting a PersistentVolumeClaim change real volume characteristics (like performance tier) after creation by referencing a new class, without needing to recreate the volume.",
 "kubernetes_storagemigration_storage_version_migration": "`kubernetes_storagemigration_storage_version_migration` triggers a real, cluster-wide rewrite of every existing object of a given resource type to its current storage version -- the mechanism for actually completing a storage version upgrade instead of leaving old objects encoded in a version the API server no longer prefers.",
 "kubernetes_storagemigration_v1beta1_storage_version_migration": "The v1beta1 shape of `kubernetes_storagemigration_storage_version_migration` -- StorageVersionMigration's own beta API generation, kept being served alongside the now-stable v1 API this cluster's own pinned schema also carries. New migrations should target the stable `kubernetes_storagemigration_storage_version_migration` instead."
}
